# HANDOFF — estado em 2026-09-25, base 6af8fb6
## Pronto (validado)
- Build 0 erros/warnings; testes 79/79.
- Limpeza pré-público: `tools/provas-winapi` e `doc/03` removidos,
  docs renomeados para inglês (`overview`, `data-model`, `macro-engine`,
  `architecture`, `business-rules`, `milestones`, `backlog`), refs
  atualizadas (AGENTS, skill, commands, docs), `.gitignore` cobre
  `.opencode/node_modules/`.
- README de produto (badges, sem Dev/Regras) + `doc/release.md`;
  `plans/` removido (runbook preservado); `doc/06` carimbado.
## Pronto (código, pendente de jogo/Windows)
- Push dos commits locais + Release v1.0.1 (feed) + teste delta fim-a-fim.
## Próximo passo
- `git push` (terminal do usuário — SSH só funciona lá) + Release v1.0.1.
## Backlog pós-validação (ponteiro p/ `doc/backlog.md`)
- Ver arquivo; U9-futuro (grade do Mini) aberto.
## Perguntas abertas
- Certificado de code signing (pago) vs SmartScreen — pendente.
