<UserControl x:Class="PwAssistant.App.Views.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:res="clr-namespace:PwAssistant.App.Resources"
             Height="32">
    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Image Width="16" Height="16" Margin="12,0,0,0" VerticalAlignment="Center"
               Source="/PwAssistant.App;component/Resources/AppIcon.png"/>
        <TextBlock Grid.Column="1" Margin="8,0,8,0" VerticalAlignment="Center"
                   FontSize="{StaticResource Font.Size.Sm}"
                   Foreground="{DynamicResource Brush.MutedForeground}"
                   TextTrimming="CharacterEllipsis"
                   Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"/>
        <StackPanel Grid.Column="2" Orientation="Horizontal"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">
            <Button x:Name="MinButton" Style="{StaticResource TitleBarButton}"
                    Content="&#xE921;" Click="OnMinimize" ToolTip="{x:Static res:Strings.Minimize}"/>
            <Button x:Name="MaxButton" Style="{StaticResource TitleBarButton}"
                    Content="&#xE922;" Click="OnMaximizeRestore"/>
            <Button x:Name="CloseButton" Style="{StaticResource TitleBarCloseButton}"
                    Content="&#xE8BB;" Click="OnClose" ToolTip="{x:Static res:Strings.Close}"/>
        </StackPanel>
    </Grid>
</UserControl>
